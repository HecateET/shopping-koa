/**
 * 控制层（处理逻辑）
 * 收获地址处理
 */
const addressDAO = require("../model/addressDAO");

module.exports = {
    //新增一个用户地址
    createAddress: async(ctx, next) =>{
        try{

        }catch(e){
            
        }
    },

    //

}